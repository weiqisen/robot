# --- 依赖与构建产物 ---
node_modules/
# 构建输出，部署时现 build，不入库
# 注意：gitignore 不支持行尾注释，注释必须单独一行，否则整行会被当成模式
studio-vue/dist/
__pycache__/
*.pyc
.venv/

# --- 运行时产物（机器上生成、和具体那台车绑定，不该跟着代码走）---
# 舵机映射、地面标定 cam_fix、投放区坐标都在这个文件里
snack_butler_config.json
*.log

# --- 本地环境 ---
.env
*.env
!*.env.example
.DS_Store

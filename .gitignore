# --- 依赖与构建产物 ---
node_modules/
studio-vue/dist/          # 构建输出，部署时现 build，不入库
__pycache__/
*.pyc
.venv/

# --- 运行时产物（机器上生成，机器相关，不该跟着代码走）---
# 舵机映射、地面标定 cam_fix、投放区坐标都在这里面，换一台车就不一样
snack_butler_config.json
*.log

# --- 本地环境 ---
.env
*.env
!*.env.example
.DS_Store
